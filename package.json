{
  "name": "mono-postgres",
  "version": "0.0.1",
  "description": "PostreSQL module for Mono",
  "main": "lib/",
  "files": [
    "lib"
  ],
  "scripts": {
    "lint": "eslint lib/**",
    "test": "npm run lint && nyc ava --verbose --serial test/ && nyc report --reporter=html",
    "coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AdminLP/mono-postgres"
  },
  "author": "Vasiliy Kryuchkov",
  "nyc": {
    "include": [
      "lib/"
    ]
  },
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/AdminLP/mono-postgres/issues"
  },
  "homepage": "https://github.com/AdminLP/mono-postgres#readme",
  "dependencies": {},
  "devDependencies": {
    "@terrajs/mono": "^0.8.0",
    "mono-test-utils": "^1.2.3",
    "ava": "^0.24.0",
    "codecov": "^3.0.0",
    "eslint": "^4.13.0",
    "nyc": "^11.3.0"
  },
  "keywords": [
    "mono",
    "mono-module"
  ]
}
